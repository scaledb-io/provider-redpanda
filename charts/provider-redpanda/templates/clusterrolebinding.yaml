{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "provider-redpanda.fullname" . }}
  labels:
    {{- include "provider-redpanda.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "provider-redpanda.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "provider-redpanda.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
